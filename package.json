{
  "name": "mini-maple",
  "private": true,
  "description": "Dev-only tooling for mini-maple's unit test suite. The shipped game itself has no build step (see README/CLAUDE.md) — this package.json exists solely to run tests.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.10"
  }
}
